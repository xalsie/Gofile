{
  "name": "gofile",
  "version": "1.0.0",
  "description": "Simple API for uploading files to Gofile.com",
  "license": "ISC",
  "author": "LeGrizzly",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "npm run build && node dist/test.js",
    "example": "npm run build && node dist/example.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.12",
    "typescript": "^5.8.3"
  }
}
